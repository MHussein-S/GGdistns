
#...
MODEL LR1 {
    for(i in 1:N) {
    sepal[i] ~ dnorm(mu[i], tau)
      mu[i] <- alpha + beta*petal[i]
    }
alpha ~ dnorm(0,0.001)
beta ~ dnorm(0,0.001)

tau <- pow(sigma, -2)
sigma ~ dunif(0,10)
}

